name: My First Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "greeting"
      - name: print good bye
        run: |
          echo "Byee"
          echo "Multi Line Shell Commands"
